# 立替回収トラッカー — プロジェクト固有ルール

> **前提**：作業前に `C:\Users\hiro2\ai-rules\CODING_RULES.md`（共通ルール）を読むこと。
> このファイルには、そこに書かれていない**このアプリ固有の事情だけ**を書いている。

---

## 1. アプリの概要

友人同士の立替金を記録し、誰からいくら回収できていないかを管理するスマホ向けウェブアプリ。
**複数ユーザーが各自のアカウントで使う**。利用者ごとにデータは完全に分かれている。

- 利用者はメールアドレスとパスワードでログインする
- 新規登録には**招待コード**（管理者が決めた合言葉）が必要
- データは管理者（スプレッドシートの持ち主）のスプレッドシートに保存される。**この点は利用者に伝えたうえで運用する**

### 構成（v3 以降。v2 から大きく変わっている）

```
[利用者のブラウザ]                    [Google]
GitHub Pages                          Apps Script          スプレッドシート
https://hirokode.github.io/           doPost()        ←→   メンバー_u1
  warikan-note/                         ↑                   立替_u1
  index.html          ── fetch ────────┘                    回収明細_u1
                                                            （利用者ごとに3枚）
```

- **画面は GitHub Pages にある。** Apps Script はもう画面を配信していない（`doGet()` は案内文を返すだけ）
- そのため **`google.script.run` は使えない。** `fetch()` で `/exec` を叩く
- Apps Script プロジェクトはスプレッドシートに紐付いた**コンテナバインド型**。`SpreadsheetApp.getActiveSpreadsheet()` で対象シートに繋がる

---

## 2. ファイル構成と役割

| ファイル | 役割 | 置き場所 |
| --- | --- | --- |
| `index.html` | 画面のすべて。HTML・CSS・JavaScript・アイコンを1ファイルに同梱 | **GitHub Pages** |
| `api.gs` | サーバー側。ログイン処理とスプレッドシートの読み書き | **Apps Script** |
| `appsscript.json` | Apps Scriptの設定 | Apps Script |
| `.claspignore` | clasp が送るのは `api.gs` と `appsscript.json` だけ、という指定 | ローカル |
| `.clasp.json` | スクリプトIDを保持。**gitの管理外** | ローカルのみ |
| `立替アプリURL.txt` | デプロイIDとスクリプトIDの控え。**gitの管理外** | ローカルのみ |

GitHubリポジトリは `hirokode/warikan-note`（Public）。リポジトリ直下がそのまま Pages の公開対象。
`git push` すると1〜2分で反映される。**画面側の変更に clasp は関係ない。**

### 2つの「反映」を混同しないこと

| 変えたファイル | 反映方法 |
| --- | --- |
| `index.html` | `git push` → GitHub Pages が自動で更新 |
| `api.gs` / `appsscript.json` | `clasp push` → `clasp deploy` |

---

## 3. 秘密情報の扱い（重要・共通ルールからの変更点）

**このプロジェクトでは `/exec` のURLは秘密情報として扱わない。**
画面のJavaScriptに書く必要があり、ブラウザが読む以上どうやっても隠せないため。
`index.html` の `API_URL` に平文で書かれており、これは意図した状態。

代わりに、**次の2つは絶対にコードやgitに入れない。** どちらも Apps Script の
「プロジェクトの設定 → スクリプト プロパティ」にだけ置く。

| プロパティ名 | 中身 | 設定方法 |
| --- | --- | --- |
| `PEPPER` | パスワードに混ぜる秘密の値 | `setupMultiUser()` が自動生成する。**人が見る必要はない** |
| `INVITE_CODE` | 新規登録に必要な合言葉 | 管理者が手で設定する。変更も手で行う |

リポジトリは Public なので、`api.gs` の中身は誰でも読める。
**認証の仕組みが公開されていても安全である設計**にしてあり、守っているのは上の2つの値とパスワードそのもの。

---

## 4. ログインの仕組み

### パスワードの保存

元に戻せない形（ハッシュ）に変換して保存する。生のパスワードはどこにも残らない。

1. 利用者ごとにランダムな**ソルト**を作る
2. `ソルト + パスワード + PEPPER` を SHA-256 で **2000回**掛け直す
3. その結果だけを「ユーザー」シートに保存する

ソルトは総当たり表を使えなくするため、PEPPER はシートが漏れても解読できないようにするためのもの。
反復回数は利用者ごとにシートへ記録しているので、将来増やしても既存アカウントはログインできる。

### ログイン状態の維持

- ログインすると**トークン**（長いランダム文字列）が発行され、ブラウザの `localStorage` に保存される
- サーバー側の「セッション」シートには、**トークンそのものではなくハッシュ**を保存する
- 有効期間は **90日**。`SESSION_DAYS` で変更できる
- データを触る処理はすべて `uidFromToken_()` を通り、トークンから利用者を確定させる。**画面から送られてきた利用者IDは信用しない**

### 管理用シート

| シート | 列 |
| --- | --- |
| ユーザー | ユーザーID / 名前 / メール / ソルト / パスワードハッシュ / 反復回数 / 登録日時 / 最終ログイン |
| セッション | トークンハッシュ / ユーザーID / 発行日時 / 有効期限 |

`cleanupSessions()` で期限切れを掃除できる。時間主導トリガーに入れてもよい。

---

## 5. スプレッドシートの構成

**利用者ごとに3枚のシートを持つ。** シート名は `<種類>_<ユーザーID>`（例：`立替_u1`）。
ユーザーIDは `u1`, `u2`, … と順番に振られる（スクリプトプロパティ `UID_SEQ` で採番）。

**列構成と既存データは絶対に壊さないこと。** 以下は v2 から変更なし。

### メンバー_uN

| 列 | 内容 |
| --- | --- |
| 名前 | 主キー扱い。重複不可 |
| 区分 | `社会人` / `学生` / 空 |
| 性別 | `男` / `女` / 空 |

**行の並び順がそのまま画面上の表示順**になる。並べ替え機能はこの行順を書き換えている。

### 立替_uN

| 列 | 内容 |
| --- | --- |
| ID | 8桁の英数字 |
| 日付 | `yyyy-MM-dd` |
| 内容 | 必須 |
| 総額 | **空欄可** |
| 割る人数(自分含む) | 選んだ人数＋（自分を含める場合は1） |
| メモ | 任意 |
| 登録日時 | 登録時に自動。**編集しても書き換えない** |

### 回収明細_uN

| 列 | 内容 |
| --- | --- |
| ID / 立替ID / 相手 / 金額 / 回収済み / 回収日 | |

### 扱ううえでの注意

- **立替を編集すると、その立替の回収明細はいったん削除して入れ直される。**
  このとき相手の名前が同じなら **明細ID・回収済み・回収日を引き継ぐ**。この引き継ぎを壊すと、金額を直しただけで回収済みのチェックが全部消える
- メンバーを削除しても、過去の回収明細は残る
- メンバーの名前を変更すると、回収明細の「相手」列も追随して書き換わる
- 更新系はすべて `withLock_()` で囲む

---

## 6. 画面とサーバーのやりとり

### 通信の形

`fetch` で `/exec` に POST する。**`Content-Type` は必ず `text/plain;charset=utf-8`。**
`application/json` にするとブラウザが事前問い合わせ（preflight）を行い、Apps Script が応答できずに失敗する。

送る形： `{ action: '関数名', args: [...], token: 'ログイン合鍵' }`
返る形： `{ ok: true, data: ... }` または `{ ok: false, error: 'メッセージ' }`

`api.gs` 側は `doPost()` → `route_()` で振り分ける。**新しい処理を足したら `route_()` にも1行足すこと**（忘れると「知らない操作です」になる）。

### 保存を待たせない仕組み（v2 から継続）

`save(ローカル更新, 関数名, 引数, メッセージ)` を通す。

1. 手元の `DATA` を先に書き換える
2. すぐ `render()` して画面に反映する
3. 保存は行列に積んで、裏で1件ずつ順番に送る
4. 行列が空になった時だけ、サーバーの返事で `DATA` を上書きする
5. 失敗したら行列を捨て、`getData()` で画面をサーバーの状態に戻す
6. **ログイン切れのエラーなら `signOut(true)` でログイン画面に戻す**

`call()` は結果を待つもの専用（最初の読み込みとエラー復帰）。更新操作をここに足してはいけない。

### 新しい更新機能を足すときの手順

1. `api.gs` に関数を追加する。**第1引数は `uid`、最後は必ず `getData(uid)` を返す**
2. `route_()` に `case` を1行足す
3. `index.html` の `click` リスナーに `case` を足し、`save()` で呼ぶ
4. `save()` の第1引数に、**サーバーと同じ結果になるローカル更新**を書く

---

## 7. デプロイ手順

### 画面（index.html）を変えたとき

```
cd "C:\Users\hiro2\OneDrive\04_Claude Code\Flair\立替記録"; git add -A; git commit -m "<変更メモ>"; git push
```

1〜2分で `https://hirokode.github.io/warikan-note/` に反映される。確認は `Ctrl + F5`。

### サーバー（api.gs）を変えたとき

```
cd "C:\Users\hiro2\OneDrive\04_Claude Code\Flair\立替記録"; clasp push -f; if ($?) { clasp deploy -i <デプロイID> -d "<変更メモ>" }
```

- `<デプロイID>` の実際の値は `立替アプリURL.txt` にある
- ターミナルは Windows PowerShell 5.1 のため `&&` は使えない
- ユーザーには **bashタグのコードブロック**で提示する（▶ボタンが付く）。**エージェントが代行実行しない**

### ウェブアプリの公開設定

**「アクセスできるユーザー」は必ず「全員」**。これを「自分のみ」に戻すと、
友人のブラウザから `fetch` できなくなりアプリが動かなくなる。
「次のユーザーとして実行」は**「自分」**（管理者の権限でスプレッドシートを読み書きするため）。

---

## 8. 現在の状態

2026-09-22 時点。

### 実装済みの機能

**ログイン / 新規登録** — 招待コード必須。90日間ログイン状態を保持
**未回収タブ** — 相手ごとの未回収合計。まとめて回収済みにできる
**立替一覧タブ** — 回収済みの切り替え、編集・削除、コピーして作成
**＋登録タブ** — 立替の登録と編集。属性での絞り込み、均等割り、属性ごとの金額指定
**設定タブ** — メンバー管理（並べ替え含む）、配色テーマ、フォント、ログアウト

### 設計上、意図してそうなっている点（変更しないこと）

- **金額は自動で入らない。** 「均等で割る」ボタンを押したときだけ均等額が入る
- **「属性ごとに入力」は均等割りの補助**。排他のモード切り替えではない
- **総額は空欄でも登録できる。** 空のときは「自分の負担」を表示しない
- **下部のバーは動かない。** `main` だけがスクロールする構造。この構造を崩さないこと
- **ローディングは右上の小さな回転サークル**
- **未ログイン時は下部バーを隠し、ログイン画面だけを表示する**

### 管理者が行う運用作業

**アプリ内に管理画面はない。** 以下はすべて Apps Script エディタから手で実行する。
`diagnose()` `moveDataTo()` `resetPassword()` は画面からは呼べない（`route_()` に登録していない）。

| やりたいこと | 方法 |
| --- | --- |
| 今の状態を調べる | `diagnose()` を実行。シート一覧・件数・登録済みアカウント・採番カウンタが出る |
| 招待コードの変更 | スクリプト プロパティの `INVITE_CODE` を書き換える |
| パスワードの再設定 | プロパティに `RESET_EMAIL` と `RESET_PW` を入れて `resetPassword()` を実行。<br>**実行後、この2つは自動削除される**（パスワードをコードにもgitにも残さないため） |
| データを別アカウントに移す | `moveDataTo(移動元, 移動先)`。例 `moveDataTo('u1', 'u2')`。<br>移動先にデータがあれば安全のため中止する。シート名の変更のみでセルは触らない |
| 利用者の削除 | 「ユーザー」シートの該当行、「セッション」シートの該当行、その人の3シートを手で削除 |

**パスワード再設定のUIは意図的に作っていない。** 自己リセットにはメール送信が必要で、
送信元が管理者のGmailになること・迷惑メールに入りやすいことから、
利用者が少人数のうちは手動対応のほうが確実だと判断した。利用者が増えたら再検討する。

---

## 9. 動作確認の方法

ブラウザを開かずにできる確認は、先に済ませること。

`index.html` の JavaScript の構文チェック：

```
sed -n '/^<script>/,/^<\/script>/p' index.html | sed '1d;$d' > check.js && node --check check.js
```

`api.gs` の構文チェック：

```
cp api.gs check_api.js && node --check check_api.js
```

サーバー側の動作は `clasp open` でエディタを開き、関数を選んで実行する。
エラーは左サイドバーの「実行数」画面で詳しく見られる。
**通信の失敗はブラウザの開発者ツール（F12）の Console と Network で確認する。**
