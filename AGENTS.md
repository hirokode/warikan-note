# 立替回収トラッカー — プロジェクト固有ルール

> **前提**：作業前に `C:\Users\hiro2\ai-rules\CODING_RULES.md`（共通ルール）を読むこと。
> このファイルには、そこに書かれていない**このアプリ固有の事情だけ**を書いている。

---

## 1. アプリの概要

友人同士の立替金を記録し、誰からいくら回収できていないかを管理するスマホ向けウェブアプリ。

- 「誰に」「いくら」立て替えたかを登録し、回収できたらチェックしていく
- 割り勘の計算（均等割り・属性ごとの金額指定）を補助する
- 利用者は本人のみ（ウェブアプリの公開設定は `access: MYSELF`）
- **スマホでの利用が主**。UIはモバイル前提で作られている

Googleスプレッドシートに紐付いた**コンテナバインド型**のApps Scriptプロジェクト。
そのため `SpreadsheetApp.getActiveSpreadsheet()` だけで対象シートに繋がる（ID指定は不要）。

---

## 2. ファイル構成と役割

| ファイル | 役割 |
| --- | --- |
| `api.gs` | サーバー側。スプレッドシートの読み書きと `doGet()` を担当 |
| `index.html` | 画面のすべて。HTML・CSS・JavaScript を1ファイルに同梱 |
| `appsscript.json` | Apps Scriptの設定（タイムゾーン、ウェブアプリの公開設定） |
| `.clasp.json` | スクリプトIDを保持。**gitの管理外**（秘密情報のため） |
| `立替アプリURL.txt` | URL・デプロイID・スクリプトIDの控え。**gitの管理外** |
| `AGENTS.md` / `CLAUDE.md` / `.agents/rules/project.md` | AIエージェント向けの指示 |

### `api.gs` の主な関数

画面から `google.script.run` で呼ばれるもの：

| 関数 | 役割 |
| --- | --- |
| `getData()` | 全データ（メンバー・立替・回収明細）をまとめて返す。**すべての更新系関数は、最後にこれを返す** |
| `addMember(m)` / `updateMember(m)` / `deleteMember(name)` | メンバーの追加・編集・削除 |
| `reorderMembers(names)` | メンバーの並び順を入れ替える（行の並べ替えのみ） |
| `addAdvance(p)` / `updateAdvance(p)` / `deleteAdvance(id)` | 立替の登録・編集・削除 |
| `setPaid(shareIds, paid)` | 回収済み／未回収の切り替え（複数まとめて可） |

内部用の小道具（末尾にアンダースコア）：`sheet_` `rows_` `date_` `newId_` `withLock_` `memberRowIndex_`

`setup()` は初回に1度だけ実行する関数。3つのシートを作る。

### `index.html` の構造

1つのファイルだが、中は大きく次に分かれている。

- **CSS**（`<style>` 内）— 配色は `:root` のCSS変数にまとまっている
- **状態** — `DATA`（サーバーから取得した全データ）、`TAB`（表示中のタブ）、`form`（登録フォームの入力状態）、`editing`（メンバー編集中の状態）
- **通信** — `call()` と `save()`（後述）
- **描画** — `render()` が `TAB` を見て `renderOwed` / `renderList` / `renderAdd` / `renderMembers` のどれかを呼び、`#view` の中身を丸ごと差し替える
- **イベント** — `document` に対する `input` / `change` / `click` / `pointer*` の委譲リスナー。個別の要素にリスナーは付けない

**画面は毎回まるごと描き直す方式。** 部分更新はしない（例外は `updateSummary()` と `refreshAmounts()` のみ）。
ボタンには `data-act="○○"` を付け、`click` リスナーの `switch` で処理を分岐させる。

---

## 3. スプレッドシートの構成

シートは3枚。**列構成と既存データは絶対に壊さないこと。**

### メンバー

| 列 | 内容 |
| --- | --- |
| 名前 | 主キー扱い。重複不可 |
| 区分 | `社会人` / `学生` / 空 |
| 性別 | `男` / `女` / 空 |

**行の並び順がそのまま画面上の表示順**になる。並べ替え機能はこの行順を書き換えている。

### 立替

| 列 | 内容 |
| --- | --- |
| ID | 8桁の英数字。`newId_()` が生成 |
| 日付 | `yyyy-MM-dd` |
| 内容 | 必須 |
| 総額 | **空欄可**。未入力のまま登録できる |
| 割る人数(自分含む) | 選んだ人数＋（自分を含める場合は1） |
| メモ | 任意 |
| 登録日時 | 登録時に自動。**編集しても書き換えない** |

### 回収明細

| 列 | 内容 |
| --- | --- |
| ID | 明細ごとの8桁ID |
| 立替ID | 「立替」シートのIDと紐づく |
| 相手 | メンバーの名前 |
| 金額 | |
| 回収済み | `true` / `false` |
| 回収日 | 回収済みにした日付 |

### 扱ううえでの注意

- **立替を編集すると、その立替の回収明細はいったん削除して入れ直される。**
  このとき、相手の名前が同じなら **明細ID・回収済み・回収日を引き継ぐ**。この引き継ぎを壊すと、金額を直しただけで回収済みのチェックが全部消える
- メンバーを削除しても、過去の回収明細は残る
- メンバーの名前を変更すると、回収明細の「相手」列も追随して書き換わる
- 同時書き込みで行がずれないよう、更新系はすべて `withLock_()` で囲む

---

## 4. 画面とサーバーのやりとり（重要）

GASの通信は1往復に3〜4秒かかる。そのため**保存待ちで画面を止めない設計**になっている。

### `save(ローカル更新, 関数名, 引数, メッセージ)`

更新操作はすべてこれを通す。

1. 手元の `DATA` を先に書き換える
2. すぐ `render()` して画面に反映する（ユーザーを待たせない）
3. サーバーへの保存は**行列（キュー）に積んで、裏で1件ずつ順番に**送る
4. 行列が空になった時だけ、サーバーが返した内容で `DATA` を上書きして描き直す（仮IDが本物のIDに置き換わる）
5. 保存に失敗したら行列を捨て、エラーを表示して `getData()` で画面をサーバーの状態に戻す

**新しく作ったデータには `tmpId()` の仮IDを振る。** サーバーの返事が来た時点で本物のIDに置き換わる。

### `call(関数名, 引数, メッセージ, 後処理)`

結果を待ってから画面を作るもの専用。**現在は初回読み込みとエラー復帰の2箇所でしか使っていない。**
更新操作をここに追加してはいけない（画面が3〜4秒止まるため）。

### 新しい更新機能を足すときの手順

1. `api.gs` に関数を追加する。**最後は必ず `getData()` を返す**
2. `index.html` の `click` リスナーに `case` を足し、`save()` を使って呼ぶ
3. `save()` の第1引数に、**サーバーと同じ結果になるローカル更新**を書く。ここがずれると画面とシートの内容が食い違う

---

## 5. デプロイの仕組みと実行コマンド

### 仕組み

- `clasp push` は**コードをApps Scriptプロジェクトに置くだけ**。これだけではURLの中身は変わらない
- `/exec` のURLは、**デプロイした時点のバージョンを固定で配信し続ける**
- したがって `push` と `deploy` の**両方**が必要

### 実行コマンド

ユーザーには、チャットに**bashタグのコードブロック**で提示する（▶ボタンが付く）。
ユーザー自身がそのボタンを押して実行する運用。**エージェントが代行実行しない。**

```
cd "C:\Users\hiro2\OneDrive\04_Claude Code\Flair\立替記録"; clasp push -f; if ($?) { clasp deploy -i <デプロイID> -d "<変更メモ>" }
```

- `<デプロイID>` の実際の値は **`立替アプリURL.txt` に書いてある**（このファイルには書かない）
- ターミナルは Windows PowerShell 5.1 のため `&&` は使えない。`;` と `if ($?) { }` で連結する
- 先頭の `cd` は必須。ターミナルはホームフォルダで開くため
- `-i` を外すと新しいURLが増えてしまう。既存アプリの更新では必ず付ける

### 成功の判定

`Pushed 3 files.` と `Created version N.` の**両方**が出れば成功。
片方しか出ていなければ反映されていない。

### 確認

デプロイ後、ブラウザで **`Ctrl + F5`**（強制リロード）。
通常のリロードだとGASのキャッシュで古い画面が残ることがある。

---

## 6. 現在の状態

2026-09-21 時点。公開中のバージョンは 9。

### 実装済みの機能

**未回収タブ** — 相手ごとの未回収合計。タップで内訳を開き、「全額受け取った」でまとめて回収済みにできる
**立替一覧タブ** — 登録済みの立替。名前のチップをタップで回収済み／未回収を切り替え。編集・削除ボタンあり
**＋登録タブ** — 立替の登録と編集。属性での絞り込み、均等割り、属性ごとの金額指定
**メンバータブ** — メンバーの追加・編集・削除、☰ をドラッグして並べ替え

### 設計上、意図してそうなっている点（変更しないこと）

- **金額は自動で入らない。** 人を選んだだけでは金額欄は空のまま。「均等で割る」ボタンを押したときだけ均等額が入る
- **「属性ごとに入力」は均等割りの補助**。排他のモード切り替えではなく、開閉できるオプション。どちらも同じ「個別の調整」欄に書き込む
- **総額は空欄でも登録できる。** 総額が空のときは「自分の負担」を表示しない
- **下部のバーは動かない。** ページ全体ではなく `main` だけがスクロールする構造（`body` を画面の高さに固定した縦並びにしている）。スマホでバーが揺れるのを防ぐため。この構造を崩さないこと
- **ローディングは右上の小さな回転サークル**。画面上部を横断するバーには戻さない

### 既知の注意点

- ドラッグ並べ替えは Pointer イベントで実装。端末による差が出やすい箇所
- 保存が残ったままタブを閉じようとすると確認ダイアログが出る（`beforeunload`）
- 画面とシートの内容がずれた場合は、リロードすれば必ずシートの内容に戻る

---

## 7. 動作確認の方法

ブラウザを開かずにできる確認は、先に済ませること。

`index.html` の JavaScript の構文チェック：

```
sed -n '/^<script>/,/^<\/script>/p' index.html | sed '1d;$d' > check.js && node --check check.js
```

`api.gs` の構文チェック：

```
cp api.gs check_api.js && node --check check_api.js
```

サーバー側の動作を確かめたいときは、`clasp open` でエディタを開き、関数を選んで実行する。
エラーは左サイドバーの「実行数」画面で詳しく見られる。
